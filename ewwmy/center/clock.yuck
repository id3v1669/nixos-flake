(defwidget clock []
  (box (button 
    :class "miniclock" 
    :orientation "h" 
    :onclick "${EWW_CMD} update show_calendar=true"
    (box 
      :orientation "h"
      :space-evenly false
      :spacing 10
      :halign "start"
      :orientation "h"
      datetime
    )
  ))
)